#include <stdlib.h>
#include <stdio.h>

int main(char** args)
{
	//kill  pid
	//find pid
	// terminate pid and set status to any identifier we want to symbolize ended processes
	char* vars="kill\n";
	printf("%s",vars);

}
