#include <stdlib.h>
#include <stdio.h>

int main(char** args)
{
	//request src directory from filesystem
	//find the requested file from filesystem
	//request destination directory from filesystem
	//move the requested file into dest
	//find the requested file from filesystem
	//rm file
	char* vars="mv\n";
	printf("%s",vars);

}
